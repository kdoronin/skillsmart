1.

if (!isset($ratingCount) || $ratingCount == '') {
            $ratingCount = '12500';
        }

Используется статическое значение рейтинга в случае, если парсер данных не сработал.
Так как используются данные только в одном случае и в одном конкретном модуле.
Плюс – данные в текущей ситуации будут выводиться только в случае сбоя внешнего сервиса.


2.

const TERM_SORT_VERSION = '0.1.0';

Версия плагина, использующаяся внутри конкретного плагина.
Используется константа, значение которой будет вручную изменяться при обновлении плагина.
Само число используется, в частности, для версионированния статических данных и сброса кэша в случае обновления плагина.


3.

public function get_order_key( $context = 'view' ) {
}

Функция, генерирующая цифро-буквенный номер заказа из итерируемого значения.
Используется для генерации значения каждый раз, при создании нового заказа.
